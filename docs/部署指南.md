# Crypto Tips 部署指南

## 部署概览

本指南介绍如何将 Crypto Tips 部署到生产环境。

## 前端部署 (推荐: Vercel)

### 使用 Vercel 部署

1. **安装 Vercel CLI**
```bash
npm install -g vercel
```

2. **登录 Vercel**
```bash
vercel login
```

3. **部署前端**
```bash
cd frontend
vercel --prod
```

### 使用 Netlify 部署

1. **登录 Netlify**
   访问 https://netlify.com

2. **连接 Git 仓库**
   - 选择 "New site from Git"
   - 连接你的 GitHub 仓库
   - 选择 `frontend` 目录

3. **配置构建设置**
   - Build command: `npm run build`
   - Publish directory: `dist`

## 后端部署 (推荐: Railway)

### 使用 Railway 部署

1. **访问 Railway**
   https://railway.app

2. **创建新项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库

3. **配置环境变量**
   在 Railway 项目设置中添加:
   ```
   PORT=3000
   JWT_SECRET=your-production-secret
   NODE_ENV=production
   ```

4. **配置构建**
   - Root Directory: `backend`
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`

### 使用 Render 部署

1. **访问 Render**
   https://render.com

2. **创建 Web Service**
   - 连接 GitHub 仓库
   - Root Directory: `backend`
   - Build Command: `npm install && npm run build && npm run prisma:generate`
   - Start Command: `npm start`

3. **添加 SQLite 持久化**
   - 在 Render Dashboard 添加 Disk
   - Mount Path: `/app/backend/prisma`

## 数据库部署

### 生产环境数据库选择

对于生产环境，建议使用以下数据库替代 SQLite:

**PostgreSQL (推荐)**

1. 更新 `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"
}
```

2. 更新环境变量:
```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

**使用 Supabase (免费 PostgreSQL)**

1. 访问 https://supabase.com
2. 创建项目
3. 复制数据库连接字符串
4. 更新 `DATABASE_URL`

## 环境变量配置

### 前端环境变量

创建 `.env.production`:
```env
VITE_API_URL=https://your-backend-api.com
VITE_CHAIN_ID=1
VITE_NETWORK_NAME=mainnet
```

### 后端环境变量

生产环境 `.env`:
```env
DATABASE_URL="postgresql://..."
PORT=3000
JWT_SECRET="use-a-strong-random-secret"
NODE_ENV=production
CORS_ORIGIN="https://your-frontend-domain.com"
```

## 区块链网络配置

### 测试网 (Sepolia)

```typescript
// 前端配置
const SEPOLIA_CHAIN_ID = 11155111;
```

### 主网 (Mainnet)

```typescript
// 前端配置
const MAINNET_CHAIN_ID = 1;

// 警告: 主网需要真实 ETH，请谨慎使用
```

## 安全检查清单

- [ ] 使用强随机 JWT_SECRET
- [ ] 启用 HTTPS (SSL/TLS)
- [ ] 配置 CORS 白名单
- [ ] 更新所有依赖到最新版本
- [ ] 进行安全审计
- [ ] 限制 API 请求速率
- [ ] 实现错误日志监控
- [ ] 备份数据库

## 性能优化

### 前端优化

1. **代码分割**
```typescript
// 使用动态导入
const TipForm = lazy(() => import('./components/TipForm'));
```

2. **启用压缩**
Vercel 和 Netlify 默认启用 Gzip/Brotli

3. **CDN 加速**
使用 Cloudflare 或 Vercel Edge Network

### 后端优化

1. **数据库连接池**
```typescript
const prisma = new PrismaClient({
  datasources: {
    db: {
      url: process.env.DATABASE_URL,
    },
  },
});
```

2. **添加缓存**
使用 Redis 缓存热点数据

3. **启用压缩**
```typescript
import compression from 'compression';
app.use(compression());
```

## 监控和日志

### 推荐工具

- **Sentry**: 错误追踪
- **LogRocket**: 用户会话重放
- **Google Analytics**: 流量分析
- **Uptime Robot**: 运行时间监控

### 集成示例

```typescript
// Sentry 集成
import * as Sentry from "@sentry/node";

Sentry.init({
  dsn: "your-sentry-dsn",
  environment: process.env.NODE_ENV,
});
```

## 域名配置

### 前端域名

1. 在域名提供商添加 CNAME 记录:
   - Name: `www` 或 `@`
   - Value: `cname.vercel-dns.com`

2. 在 Vercel 添加自定义域名

### 后端域名

1. 在 Railway/Render 配置自定义域名
2. 添加 A 记录指向服务器 IP

## CI/CD 配置

### GitHub Actions 示例

创建 `.github/workflows/deploy.yml`:

```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: cd frontend && npm install && npm run build

  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: cd backend && npm install && npm run build
```

## 故障排除

### 前端构建失败

检查:
- Node.js 版本是否正确
- 环境变量是否设置
- 依赖是否完整安装

### 后端连接失败

检查:
- 数据库 URL 是否正确
- 网络防火墙设置
- 端口是否开放
- 环境变量是否加载

### 钱包连接问题

确保:
- 使用 HTTPS (MetaMask 要求)
- 网络 ID 配置正确
- 用户已安装 MetaMask

## 更新和维护

### 定期更新

```bash
# 更新依赖
npm update

# 检查安全漏洞
npm audit
npm audit fix
```

### 数据库迁移

```bash
# 生产环境迁移
npx prisma migrate deploy
```

### 回滚策略

- 使用 Git 标签标记版本
- 保留最近 3 个版本的部署
- 准备快速回滚脚本

## 成本估算

### 免费方案

- Vercel (前端): 免费
- Railway (后端): 免费 $5/月额度
- Supabase (数据库): 免费 500MB
- **总计**: 免费

### 付费方案 (小规模)

- Vercel Pro: $20/月
- Railway Pro: $20/月
- Supabase Pro: $25/月
- **总计**: $65/月

## 扩展性考虑

### 水平扩展

- 使用负载均衡器
- 部署多个后端实例
- 使用 CDN 分发静态资源

### 垂直扩展

- 升级服务器配置
- 增加数据库连接数
- 优化代码性能

## 合规性

### 隐私政策

- 明确告知数据收集范围
- 遵守 GDPR (欧盟)
- 遵守 CCPA (加州)

### 服务条款

- 明确平台责任范围
- 用户行为规范
- 争议解决机制

## 支持和反馈

- GitHub Issues: 问题追踪
- Discord/Telegram: 社区支持
- Email: 技术支持

---

**祝部署顺利!** 🚀
